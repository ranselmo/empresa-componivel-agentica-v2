apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cell-notificacoes-s1-active-pdb
  namespace: poc-eci
spec:
  minAvailable: 1
  selector:
    matchLabels: { app: cell-notificacoes, shard: shard-1, role: active }
