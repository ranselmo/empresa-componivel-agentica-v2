apiVersion: apps/v1
kind: Deployment
metadata:
  name: cell-notif-s2-passive
  namespace: poc-eci
  labels: { app: cell-notif, shard: shard-2, role: passive, pbc: notif }
spec:
  replicas: 1
  selector:
    matchLabels: { app: cell-notif, shard: shard-2, role: passive }
  template:
    metadata:
      labels: { app: cell-notif, shard: shard-2, role: passive, pbc: notif }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: cell-notif
                  shard: shard-2
                  role: active
              topologyKey: kubernetes.io/hostname
      containers:
        - name: cell-notif
          image: poc-eci/cell-notif:latest
          ports: [{ containerPort: 8000 }]
          env:
            - { name: SHARD_ID, value: shard-2 }
            - { name: CELL_ROLE, value: passive }
            - { name: KAFKA_BROKERS, value: kafka:29092 }
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: cell-notif-s2-passive, key: database-url }
            - name: REDIS_URL
              valueFrom:
                secretKeyRef: { name: redis-notif-s2, key: url }
          resources:
            requests: { cpu: 100m, memory: 64Mi }
            limits: { cpu: 500m, memory: 256Mi }
          livenessProbe:
            httpGet: { path: /healthz/live, port: 8000 }
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /healthz/ready, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: cell-notif-s2-passive
  namespace: poc-eci
spec:
  selector: { app: cell-notif, shard: shard-2, role: passive }
  ports: [{ port: 8000, targetPort: 8000 }]
  type: ClusterIP
